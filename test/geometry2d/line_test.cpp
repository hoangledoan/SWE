#include "line.h"
#include "point.h"

void testCreateLineExpectTrue(){
    Point start;
    start.x = 1.0;
    start.y = 2.0;

    Angle angle;
    angle.value = 60;

    distance = 50

    const bool result = createLine(start, angle, distance)
    assert(result == )
}